{
  "extends": ["claude-code-lore"],
  "filesystem": {
    "allow": ["$HOME/.claude-work"],
    "deny": [
      "$HOME/.claude-personal",
      "$HOME/.claude",
      "$HOME/.claude.pre-split-backup"
    ]
  },
  "meta": {
    "name": "claude-code-work",
    "description": "Claude Code with shared dotfiles access for work"
  }
}
