# Contributing

This repository is shared for reference and reuse. Issues, pull requests, wiki
edits, support requests, and feature requests are not accepted.

Fork the repository if you want to adapt the configuration for your own use.

After changing `bin/.local/bin/nvme-health-report` or its fixtures, run:

```sh
tests/nvme-health-report.sh
```
